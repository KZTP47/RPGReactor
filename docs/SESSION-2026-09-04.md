# Session closeout — 2026-09-04

Development version **0.98.5**, runtime revision **20260904.17**. This entry
collects today's work, including the earlier lighting changes already recorded
in Git and the subsequent rendering, authoring, and audit fixes. Detailed
implementation history remains in [HANDOFF.md](HANDOFF.md).

## Lighting, shadows and performance

- Reduced work on lights that contribute nothing to a pixel and avoided
  duplicate anchored-effect updates. Existing static shadow caching and frame
  budgets remain central to the performance strategy.
- Stabilized shadow-light selection when several lights overlap. Authored
  flicker no longer changes slot priority; moving light origins refresh static
  and dynamic shadow rows together instead of displaying mismatched snapshots.
- Fixed spotlight flicker in database and map previews using the runtime's
  shared light animation logic.
- Measured the changes with CPU/GPU profiling, frozen images, and moving-scene
  probes, including AMD integrated graphics. The recorded spotlight comparison
  reduced full-quality GPU cost roughly 19%; this is a scene-specific result.

See [performance measurements and reproduction](PERFORMANCE.md) and
[current rendering defaults](STATUS.md#current-behavior-that-supersedes-older-notes).

## Models, first-person view and speech

- Database model switches stop previous animations/effects immediately and
  reject late preview/model loads. Right-hand animation/effect lists support
  clipboard shortcuts and Edit, Copy, Paste, Duplicate and Delete menus,
  including right-clicking blank space and empty lists.
- Face-point authoring designates eyes, mouth and lips, optionally following
  a part or bone. First-person view uses the authored eye point at the model's
  actual scale and retains the player's body and followers.
- Speak 3D Dialogue combines optional spoken text and voice audio with mouth
  movement, stop/reset behavior and interpreter waits. Speech samples a cached
  audio amplitude envelope; it does not perform phoneme recognition.
- Prop animation sequences survive main-menu return in 2D and 3D. Explicit
  stops and completed one-shots stay stopped. Placement-level animation speed
  can be changed by event command without replacing the shared animation.

See [face points, speech and playback usage](3D-FACE-AND-SPEECH.md).

## 2D runtime and map-editor previews

- Animated model sprites and carried lights now work in the flat runtime;
  the editor also previews prop motion, attached animation/video effects and
  native/carried lights outside the specialized tools.
- Corrected ambient brightness, overly opaque cones, glow placement behind
  emitters, animated-model clipping, and depth sorting for lifted props.
- Shared-context model textures remain on the GPU. Static/offscreen poses
  avoid redraws; cached animated bounds avoid per-frame vertex scans.
- The editor light field uses source height, aim and falloff from the 3D logic.
  Gradients blend smoothly, and prop shadows crop/cull/skip empty work. GPU
  comparisons covered 24 reference fields within one 8-bit level.
- Flat lighting remains an approximation: wall lighting, self-shadowing and
  event-model casters are not identical to the full 3D path.

See [flat preview measurements and limits](PERFORMANCE.md).

## Database, event commands, language and themes

- Exercised every command-picker item and all database sections, with nested
  dialog save/cancel checks. Save failures now show feedback and preserve
  drafts; corrupt model settings/bindings and empty quest choices are handled
  explicitly.
- Added 88 phrases across the 17 non-English locales, corrected translation
  inventory/interpolation gaps, and routed the 3D navigation title correctly.
  The inventory finds 3,989 routed phrases with no missing catalog entries.
- Corrected nested controls, menus, selected/hover states and warning/error
  contrast across the 14 themes.
- The state audit fixed callbacks writing into cancelled or newly selected
  records, delayed control setup, lingering previews/listeners/dialogs, obsolete
  image loads and project cache reuse. Saves freeze the database until they
  settle, and a stale completion cannot affect a newer session.
- Effekseer texture callbacks now reject released effects/contexts before
  accessing WebGL. Animation pickers and timing audio also retire on departure.
  Removed five placeholder Select All actions from single-row trait menus.

See the [editor audit](EDITOR_AUDIT.md), [database state audit](DATABASE_STATE_AUDIT.md),
and their checked-in [command matrix](audits/2026-09-04-editor-matrix.json) and
[sequence results](audits/2026-09-04-database-sequences.json).

## Documentation, recovery and Demo content

Current status, rendering/interface designs, runtime-event documentation,
release instructions, missing-asset inventory and historical supersession notes
were reconciled with the implementation. The documentation index now links the
new feature, performance and audit reports directly.

Dropbox conflict copies were recovered/archived outside the project, with both
versions retained under `/home/doug/.local/share/rpg-reactor-recovery/20260904-150237`.
The active Git index and canonical saves were preserved. No conflict-copy files
remain in the normal project file inventory.

The commit includes the owner's current Demo content: 1920×1080 screen/UI
settings, the first-person map camera, updated prop effect settings and map
placements/events, and the Graviton Pistol model with its authored beam effect.
The map now has empty event slots 2 and 20. Authored changes are retained as-is;
GUI audits use disposable copies and preserve the original open-project lock.
Canonical runtime changes and the bundled Demo's runtime copies are included.

## Validation and remaining work

- **405 live database sequence checks passed across 19 sections**, with zero
  uncaught errors. This includes all 361 ordered navigation pairs and additional
  mutation, Cancel/Apply, dialog-reuse and delayed-work checks.
- Authoring coverage: **123 command-picker items**, **19 database sections**,
  **56 nested dialogs**, and **32 menu/locale cases**. The earlier theme pass
  exercised all 14 themes. Catalog completeness does not certify native fluency.
- Latest full Node suite: **2,643 passed, 3 failed, 2,646 total**. The three
  existing `stock-interfaces.test.cjs` failures assume the former Demo title
  layout; the authored 1920×1080 change is retained. This is not a green release
  candidate. All 30 documentation/release-contract checks and syntax checks
  for 113 changed/new JavaScript files pass. Nine JSON/project files parse,
  and the five changed canonical runtime files match their Demo copies.
  Closeout logs: `/tmp/rr-20260904-closeout-tests.log` and
  `/tmp/rr-20260904-closeout-docs.log`.

Sequence coverage is representative, not every possible field/order combination.
Model sidecar edits still save immediately and are not rolled back by Database
Cancel. Missing Demo assets, sustained low-end GPU playthroughs, the owner's
fullscreen-softness report, Braver's full save/transfer flow, and native release
verification remain documented in [STATUS.md](STATUS.md#open-work-and-verification).
No release, deployment or remote publication is part of this closeout.
